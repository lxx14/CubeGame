import React, { FC } from 'react';
import { SafeAreaView, Text } from 'react-native';

export const Statistic: FC = () => {
  return (
    <SafeAreaView>
      <Text>Statistic</Text>
    </SafeAreaView>
  );
};
