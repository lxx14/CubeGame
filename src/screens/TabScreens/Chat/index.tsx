import React, { FC } from 'react';
import { SafeAreaView, Text } from 'react-native';

export const Chat: FC = () => {
  return (
    <SafeAreaView>
      <Text>Chat</Text>
    </SafeAreaView>
  );
};
