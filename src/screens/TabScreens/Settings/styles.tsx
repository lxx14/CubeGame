import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
  },
  button: {
    textTransform: 'uppercase',
    marginTop: 20,
  },
});

export { styles };
